#include <string>

class writeFile {
	public:
		static void readInputUser(int [], std::string [], float []); //Reads info from the user
		static void writeFileUser(); //Writes the readed info to a file
		static const int length = 5; // Length of the products list
};
