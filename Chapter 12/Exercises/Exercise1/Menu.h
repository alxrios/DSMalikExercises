#include "RomanType.h"

class Menu {
	public:
		static void display(RomanType&); 
			// Calls the corresponding function
			// according to user option
		static void text(); 
			// Menu's options text
};
