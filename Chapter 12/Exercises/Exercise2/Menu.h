#include "DayType.h"

class Menu {
	public:
		static void display(DayType);
			// Displays the options and calls the adequate metod
		static void text();
			// Text options for the display function
};
