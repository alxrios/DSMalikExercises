#include "utilities.h"

class Menu {
	public:
		static void display(BooksShelf &);
			// Displays the menu and applies the desired option
	private:
		static void text();
			// Text to be displayed at menu display call
};
