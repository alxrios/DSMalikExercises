#ifndef H_write
#define H_write
#include "group.h"

class Write {
	public:
		static void writeFile(Group &);
			// Writes the information to the filebuf
			// as required by the exercise instructions
};

#endif